# MongoDB Connection
MONGODB_URI=mongodb://localhost:27017/bill-demo
# For MongoDB Atlas use: mongodb+srv://username:password@cluster.mongodb.net/bill-demo

# JWT Configuration
JWT_ACCESS_SECRET=your-super-secret-access-key-change-this
JWT_REFRESH_SECRET=your-super-secret-refresh-key-change-this
JWT_ACCESS_EXPIRE=15m
JWT_REFRESH_EXPIRE=7d

# Server Port
PORT=5000

# Node Environment
NODE_ENV=development

# Email Configuration (SMTP)
# For Gmail: smtp.gmail.com, port 587, use app-specific password
# For SendGrid: smtp.sendgrid.net, port 587
# For Mailtrap (testing): smtp.mailtrap.io, port 2525
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-specific-password # f3dde739f862a1
SMTP_FROM=noreply@billdemo.com
SMTP_FROM_NAME=Bill Demo

# Frontend URL (for password reset links)
FRONTEND_URL=http://localhost:3000

# var transport = nodemailer.createTransport({
#   host: "sandbox.smtp.mailtrap.io",
#   port: 2525,
#   auth: {
#     user: "14208185012723",
#     pass: "f3dde739f862a1"
#   }
# });
